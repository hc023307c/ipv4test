# IP 子網路計算網站工作計畫

## 專案目標
- 建立一個可於瀏覽器操作的 IPv4 子網路計算網站。
- 使用者可輸入：
  - IP 位址
  - CIDR 首碼，例如 `/24`
  - 或子網路遮罩，例如 `255.255.255.0`
- 網站需輸出：
  - 子網路遮罩
  - 網段名稱
  - 廣播位址
  - IP 網段範圍
  - 可用 IP 範圍
  - 可用主機數
- 前端檔案需拆分為：
  - `index.html`
  - `style.css`
  - `app.js`
- 視覺風格方向：
  - 吉普力風
  - 溫暖、自然、手作感
  - 操作清楚、閱讀舒適

## 工作 SOP 勾選清單

### 1. 規劃階段
- [x] 確認需求內容與輸出欄位
- [x] 確認採用純前端靜態網站形式
- [x] 定義畫面區塊與互動流程
- [x] 定義資料驗證規則（IP / 首碼 / 子網路遮罩）

### 2. 畫面設計階段
- [x] 設計 `index.html` 結構
- [x] 設計吉普力風視覺主題
- [x] 規劃輸入區、結果區、錯誤提示區
- [x] 完成桌面與手機版排版

### 3. 功能開發階段
- [x] 建立 IP 格式驗證
- [x] 建立 CIDR 首碼驗證
- [x] 建立子網路遮罩驗證
- [x] 實作首碼與子網路遮罩互轉
- [x] 計算網段位址
- [x] 計算廣播位址
- [x] 計算整體 IP 網段範圍
- [x] 計算可用 IP 範圍
- [x] 顯示網段名稱（例如 `192.168.1.0/24`）
- [x] 顯示可用主機數
- [x] 處理 `/31`、`/32` 特殊情況

### 4. 體驗優化階段
- [x] 補上清楚的錯誤訊息
- [x] 增加輸入提示與範例
- [x] 增加按鈕互動與結果呈現細節
- [x] 確保結果區在無資料時也有良好版面

### 5. 測試驗證階段
- [x] 以程式邏輯完成主要案例檢查
- [x] 檢查常見案例：`192.168.1.10/24`
- [x] 檢查遮罩輸入案例：`10.0.0.25` + `255.255.255.0`
- [x] 檢查小網段案例：`/30`
- [x] 檢查特殊案例：`/31`、`/32`
- [x] 檢查錯誤輸入格式處理
- [ ] 在本機瀏覽器進行人工完整測試

### 6. 文件與交付階段
- [x] 確認檔案已拆分為 `index.html`、`style.css`、`app.js`
- [x] 建立 `README.md`
- [x] 初始化 Git repository
- [x] 建立初始 commit
- [x] 推送專案到 GitHub
- [x] 更新工作計畫文件至最新狀態
- [x] 提供部署到 Vercel 的基本說明

## 目前產出檔案
- `work-plan.md`
- `README.md`
- `index.html`
- `style.css`
- `app.js`
- `.gitignore`

## GitHub 狀態
- Repository: `https://github.com/hc023307c/ipv4test.git`
- Branch: `main`
- 狀態：已成功推送

## 備註
- 專案使用純 `HTML + CSS + JavaScript` 製作，不依賴額外框架。
- 目前已完成可部署版本，適合直接部署到 Vercel。
- 由於本機環境限制，尚未進行瀏覽器端的完整人工測試；如部署後需要，我可以再協助做一輪上線前微調。

## 結案進度
- [x] 工作計畫建立完成
- [x] 網站功能開發完成
- [x] GitHub 上傳完成
- [x] README 補齊完成
- [x] 工作計畫更新完成

## 可複用 AI Prompt 分階段整理

以下內容是根據本次專案需求，整理成可直接提供給 AI 的分階段 Prompt。

### 階段 1：建立工作計畫

```text
請幫我做一個 IPv4 子網路計算網站。

需求如下：
1. 使用者可以輸入 IP 位址。
2. 使用者可以輸入 CIDR 首碼，或輸入子網路遮罩。
3. 系統要能輸出：
   - 子網路遮罩
   - 網段名稱
   - 廣播位址
   - IP 網段範圍
   - 可用 IP 範圍
   - 可用主機數
4. 網站前端檔案要拆成：
   - index.html
   - style.css
   - app.js
5. 網站視覺風格要做成吉普力風，溫暖自然、帶一點手作感。

請先不要直接開始寫功能。
先幫我建立一份 work-plan.md，內容要有：
- 專案目標
- 工作 SOP
- 可勾選的進度清單
- 預計產出檔案
- 測試與驗收項目

請把這份 work-plan.md 存到專案根目錄，方便我先確認工作進度。
```

### 階段 2：開始實作網站

```text
我同意 work-plan.md 的內容，請開始實作。

請直接建立並完成以下檔案：
- index.html
- style.css
- app.js

需求如下：
- 可輸入 IPv4 位址
- 可輸入 CIDR 首碼，例如 /24
- 或輸入子網路遮罩，例如 255.255.255.0
- 若同時輸入首碼與子網路遮罩，要檢查是否一致
- 輸出：
  - 子網路遮罩
  - 網段名稱
  - 廣播位址
  - IP 網段範圍
  - 可用 IP 範圍
  - 可用主機數
- 需要處理 /31、/32 等特殊情況
- 要有錯誤訊息與基本輸入驗證
- UI 風格請做成吉普力風
- 要有桌機與手機版排版

請直接完成檔案並更新 work-plan.md 的進度。
```

### 階段 3：初始化 Git 並準備上傳 GitHub

```text
請幫我把目前專案整理成可以上傳 GitHub 的狀態。

請協助我：
1. 初始化 Git repository
2. 建立適合這個專案的 .gitignore
3. 建立初始 commit
4. 若需要，將分支名稱改成 main
5. 告訴我接下來需要提供哪些 GitHub 資訊

如果有需要我提供的資料，再明確列給我。
```

### 階段 4：接上 GitHub 並推送

```text
以下是我的 GitHub repository URL：
<把你的 GitHub repo URL 放在這裡>

請幫我：
1. 設定 git remote origin
2. 推送目前專案到 GitHub
3. 確認 main 分支已成功上傳

如果推送過程中遇到權限、safe.directory 或網路限制，請直接協助我處理。
```

### 階段 5：補 README.md

```text
請幫我新增 README.md，內容要適合放在 GitHub 專案首頁展示。

README 需要包含：
- 專案介紹
- 功能特色
- 專案檔案結構
- 使用方式
- 可輸出的欄位
- 技術說明
- Vercel 部署方式
- 後續可擴充方向

完成後請幫我 commit 並推送到 GitHub。
```

### 階段 6：更新 work-plan 並結案

```text
請幫我把本地的 work-plan.md 更新到最新狀態。

需要反映以下內容：
- 網站已完成
- README 已完成
- Git 已初始化
- GitHub 已成功上傳
- 已提供 Vercel 部署說明
- 標記目前已完成與未完成項目

更新後請 commit 並推送到 GitHub，讓專案可以結案。
```

### 階段 7：整理成最終可重用 Prompt

```text
請幫我把這次專案中我下過的需求，整理成可以重複使用的 AI Prompt。

要求如下：
- 幫我依照實際開發流程分階段整理
- 每一階段都要是可直接貼給 AI 的 Prompt
- 請使用繁體中文
- 要適合網站開發、GitHub 上傳、README 補齊、Vercel 部署前準備這類工作

請把整理好的內容附加到 work-plan.md 最下方，並幫我推送到 GitHub。
```
